apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: deny-egress
spec:
  podSelector:
    matchLabels:
      egress: "deny"
  policyTypes:
  - Egress
